export const INITIAL_OPTIONS: any = {
	// 字体大小
	fontSize: 12,
	// 制表符大小
	tabSize: 2,
	// 字体类型
	fontFamily: 'Menlo, Monaco, Courier New, monospace',
	// 是否支持折叠
	folding: true,
	// 小地图配置
	minimap: {
		// 是否启用小地图
		enabled: false,
	},
	// 自动缩进方式
	autoIndent: 'advanced',
	// 是否启用右键菜单
	contextmenu: true,
	// 是否使用制表符停止
	useTabStops: true,
	// 是否基于单词的建议
	wordBasedSuggestions: true,
	// 粘贴时是否格式化
	formatOnPaste: true,
	// 自动布局
	automaticLayout: true,
	// 行号显示方式
	lineNumbers: 'on',
	// 是否换行
	wordWrap: 'off',
	// 是否允许滚动超出最后一行
	scrollBeyondLastLine: false,
	// 是否固定溢出小部件
	fixedOverflowWidgets: false,
	// 代码片段建议位置
	snippetSuggestions: 'top',
	// 滚动条配置
	scrollbar: {
		// 垂直滚动条显示方式
		vertical: 'auto',
		// 水平滚动条显示方式
		horizontal: 'auto',
		// 垂直滚动条大小
		verticalScrollbarSize: 10,
		// 水平滚动条大小
		horizontalScrollbarSize: 10,
	},
};

// 定义差异编辑器的初始选项
export const DIFF_EDITOR_INITIAL_OPTIONS: any = {
	// 字体大小
	fontSize: 12,
	// 字体类型
	fontFamily: 'Menlo, Monaco, Courier New, monospace',
	// 是否支持折叠
	folding: true,
	// 小地图配置
	minimap: {
		// 是否启用小地图
		enabled: false,
	},
	// 自动缩进方式
	autoIndent: 'advanced',
	// 是否启用右键菜单
	contextmenu: true,
	// 是否使用制表符停止
	useTabStops: true,
	// 粘贴时是否格式化
	formatOnPaste: true,
	// 自动布局
	automaticLayout: true,
	// 行号显示方式
	lineNumbers: 'on',
	// 是否换行
	wordWrap: 'off',
	// 是否允许滚动超出最后一行
	scrollBeyondLastLine: false,
	// 是否固定溢出小部件
	fixedOverflowWidgets: false,
	// 代码片段建议位置
	snippetSuggestions: 'top',
	// 滚动条配置
	scrollbar: {
		// 垂直滚动条显示方式
		vertical: 'auto',
		// 水平滚动条显示方式
		horizontal: 'auto',
		// 垂直滚动条大小
		verticalScrollbarSize: 10,
		// 水平滚动条大小
		horizontalScrollbarSize: 10,
	},
};
